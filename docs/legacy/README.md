# Legacy `husky-snow` Backup

This folder preserves a redacted copy of the older single-file prototype from `matthewdholtkamp/husky-snow` before repo consolidation.

The backup is for reference only. It keeps the original gameplay flow and prompt shape available while removing exposed Google API-key values.
